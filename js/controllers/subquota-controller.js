angular.module('serenata-de-amor-visualization').controller('SubquotaController', function ($scope, $http) {
  $scope.update_radar = function(person) {    
    update_radar(person);    
  };
  
});

function update_radar(person) {
}
